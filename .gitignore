# ==============================
# macOS
# ==============================
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# ==============================
# Xcode / Swift (MiaoJiAccout)
# ==============================
build/
DerivedData/
.derived-data/
.derived-data-*/
*.moved-aside
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
!default.pbxuser
!default.mode1v3
!default.mode2v3
!default.perspectivev3

## Xcode workspace / project user state (keep the shared workspace file, ignore per-user data)
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

## Obj-C / Swift specific
*.hmap

## Local app configuration
# The production client config is checked in so Xcode Cloud archives receive it.
# Publishable/anon keys are public client credentials; server secrets remain ignored.

## Playgrounds
timeline.xctimeline
playground.xcworkspace

## Swift Package Manager
.build/
.swiftpm/

## CocoaPods (uncomment if the project adopts CocoaPods)
# Pods/

## Carthage (uncomment if the project adopts Carthage)
# Carthage/Build/

## fastlane (uncomment if fastlane is added later)
# fastlane/report.xml
# fastlane/Preview.html
# fastlane/screenshots/**/*.png
# fastlane/test_output

# ==============================
# Local prototypes / design drafts
# ==============================
node_modules/
dist/
.cache/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

## Prototype folders at any depth (draft UI, not part of the shipped app)
**/prototype/
**/prototype_*/

# ==============================
# Local tool metadata
# ==============================
.agents/
.codex/
.cursor/
.claude/
*.code-workspace

# ==============================
# Archives
# ==============================
*.zip
*.rar
*.7z
*.tar
*.tar.gz
*.tgz
归档.zip

# ==============================
# Editor / IDE
# ==============================
.vscode/
.idea/
*.swp
*~

# Environment files may contain credentials. Commit templates only.
.env
.env.*
*.env
*.env.*
!.env.example
!*.env.example

# Private keys, signing identities, and provisioning profiles
*.pem
*.key
*.p8
*.p12
*.pfx
*.cer
*.mobileprovision
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*

# Common local credential/config files
credentials.json
service-account*.json
secrets.yml
secrets.yaml
secrets.json
venv/

# De-identified model evaluation inputs and generated reports stay local.
/server/test-recordings/
/server/evaluations/
/server/evaluation-manifest.json
/server/pricing.json

# Supabase CLI local link and runtime metadata
/supabase/.temp/

# ==============================
# Maintainer-only docs (local ops, filing, release notes)
# 默认忽略 docs/ 下的全部 Markdown，只放行策略站点与 README 需要的文档，
# 新增的运维/发布/备案类文档无需再逐个添加规则。
# ==============================
HANDOFF.md
docs/*.md
# 策略站点构建来源（client/scripts/build_policy_site.mjs）
!docs/privacy-policy.md
!docs/terms-of-service.md
!docs/support.md
# README / 开发流程引用
!docs/qwen3.5-omni-migration-and-evaluation.md
!docs/supabase-migration-guide.md
!docs/xcode-cloud.md

# ==============================
# docs/assets 素材（结构对齐 cijing 项目）
# 入库的只有 README 展示截图、Logo 和站点资源，其余素材目录一律本地保留。
# ==============================

# App Store Connect 上传用截图（约 49MB，由 scripts/capture-app-store-screenshots.sh 生成）
/docs/assets/app-store-connect/
# 备案素材：备案后台截图、结果截图、域名证书、提交的运行截图
/docs/assets/icp-filing/
/docs/assets/public-security-filing/

# 证书与证件材料存放在仓库之外的 ../miaoji-data/；这条是误放进 docs/ 时的兜底。
/docs/miaoji-data/
*.cer
*.mobileprovision
*.p12
*.mobileprovision.plist

# Deployment configs (server-specific)
deploy/nginx/